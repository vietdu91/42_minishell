|| si la premiere commande ne s'execute pas fait la deuxieme sinon ne la lance jamais

yo||pwf||ls | wc -l cmd 
	bash: yo: command not found
	bash: pwf: command not found

"env'''grrr"'c' | df|"df"

"env'''grrr"'c' | df|"df sd << >>> bouh'"
"$A'$B"'$C"$D'$E'"$F'"'$G'$H"
""''echo hola""'''' que""'' tal""''

e"ch"o -> 0

ech"o | env" -> error
ech"o" | "env" -> 0

"e'ch'o | env"
"e'ch'o"

echo "bite lol" 'ptdr "lmfao'|<<>> grep -c "xla" <Makefile>|loooooool




le premier mot de la ligne est interprété comme le nom de la commande, 
les autres mots en sont les paramètres ou options ;

la fin de la commande est marquée, soit par un « ; » 
(si l'on veut placer plusieurs commandes sur la même ligne), soit par un saut de ligne.

0- guetter gestion de flux permet de savoir ou sera le fichier
< fichier cmd (on utilise qu'un seul fichier puis fichier) 
impossible de renvoyer vers plusieurs fichier
1- builtin
2- access cmd
3- cmd externes
|||\\\yo||||||||hello.c|kola|loko<duf>fud

je repere lequel est un METACHAR
je modifie le content du dernier noeud 
j'ajoute le METACHAR a utiliser 
ajout du content suivant

list_end avec les elements du split 
on garde un pointeur sur le premier element 
ajout des meta depuis le pointeur avec un list insert 
word counter 

if (is-meta)
	if (str[i] == '|' && str[i + 1] == '|')
	{
		list_insert(l, "||")
		i++;
	}
	else
		list_insert(l, str[i])
else
	while (!is_meta)
		i++;

